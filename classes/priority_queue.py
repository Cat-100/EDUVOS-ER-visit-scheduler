class PriorityQueue:
    ''' 
    
    Priority Queue that utilizes the Binary Heap tree structure to queue, dequeue, 
    and ensure quick retrieval of `patients` based on their `priority level` 
    
    **Why Binary Heap tree structure?**
    
    Lists commonly ustilize contingious memory blocks, meaning that arrays need to be appended 
    and unneccessary memory blocks are used to grow an list/array dynamically.

    Since the 
    '''